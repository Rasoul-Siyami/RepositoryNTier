@model Matrix.Company.DomainClasses.About
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/Admin/_AdminLayout.cshtml";
}
<h2>
    بخش مدیریت | حذف درباره ما</h2>
@*
<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>About</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Description)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Description)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Status)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Status)
    </div>
</fieldset>*@
@using (Html.BeginForm(actionName: "Delete", controllerName: "Anout", routeValues: new { id = Model.Id }))
{
    <p>
        <input type="submit" value="حذف" onclick="return confirm ('آیا می خواهید رکورد موردنظر را حذف کنید؟');" /><br />
        @Html.ActionLink("برگشت به لیست وب لینک ها", "IndexAdmin")
    </p>
}