@model IEnumerable<Matrix.Company.DomainClasses.About>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@foreach (var item in Model)
{
    <p>
        @Html.DisplayFor(modelItem => item.Description)
    </p>

}